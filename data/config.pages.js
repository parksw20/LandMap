// GitHub Pages 배포용 VWorld 공개 키 (커밋되는 파일 — 로컬 키와 별개)
// 생성: keyring에 'v-world'/'parksw20-pages' 키 저장 후 `python make_config.py`
// 로컬(config.local.js)이 이미 키를 설정했으면 아무것도 하지 않음.
if (!window.VWORLD_KEY) {
    // 아직 Pages용 키 미설정 — VWorld 기능(건물연령/이용계획/필지조회)은 로컬 서버에서만 동작
}

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/

# VS Code
.vscode/

# OS
.DS_Store
Thumbs.db

# Project specific
*.zip
*.csv
!LAWD_서울_경기.csv

# Data files (excluding cache and config)
data/2025/
data/2026/
!data/address_cache.json
!data/manifest.json
!data/*.js
!data/*.html
!data/*.css
!data/*.json

# 로컬 전용 설정 (API 키 포함 — 커밋 금지)
data/config.local.js
finish_all.log
ledger_auto.log

# 로컬 전용 관리 콘솔 — 공개 사이트에 배포하지 않음
data/panel.html
panel_job.log

# 건축물대장 수집 캐시 — 앱이 안 씀(파생물 supply_area/bldg_ratio만 사용).
# 70MB+로 계속 커져 GitHub 한도 초과 → 로컬 재개용으로만 유지, 커밋 제외.
data/ledger_cache/
